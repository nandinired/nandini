version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - python --version
  pre_build:
    commands:
      - export STARTTIME=`date +%s`
      - export BUILDNAME="$GIT_BRANCH-`date +%Y-%m-%d`"
      - export BRANCH="$GIT_BRANCH"
      - export SOURCE_VERSION="$CODEBUILD_SOURCE_VERSION"
      - export FUNCTION_NAME="$LAMBDA_TO_UPDATE"
      - export username="$GITUSER"
      - export pass="$GITPASSWORD"
      - echo "BRANCH $BRANCH"
      - echo "SOURCE_VERSION $SOURCE_VERSION"
  build:
    commands:
      - echo Build started on `date`
      - ls
      - rm -rf *
      - ls
      - pwd
      - git clone https://Webdevlive:329gvqbu42@github.com/ForeverCompanies/magento2.git 
      - ls
      - pip install dirsync
      - cp -p -r --backup=numbered `find ~/ -name dirsync -a -type d` ./
      - ls
      - zip -qr source.zip ./
      - aws s3 sync source.zip s3://test-buildartifactsbucket/source.zip
      - aws lambda update-function-code --function-name test --zip-file fileb://source.zip
      - sleep 3
      #- aws sns publish {} topic
  post_build:
    commands:
      - echo Build completed on `date`
      - pwd
      - ls -al
artifacts:
  files:
    - '**/*'

